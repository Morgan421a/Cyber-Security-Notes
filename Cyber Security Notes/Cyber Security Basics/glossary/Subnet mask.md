32-bit number that splits an IP address into the network portion and the host portion; tells the device which part of the IP identifies the subnet.

Also defines the max number of hosts the subnet can support.