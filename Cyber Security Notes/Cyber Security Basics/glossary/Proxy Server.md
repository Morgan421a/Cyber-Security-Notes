---
aliases:
  - Proxy
---
- Proxy Server

- Intermediary system that sits between a client and a destination server. Instead of connecting directly to the internet, the client sends requests to the proxy, which forwards them to the target server and returns the response on the client's behalf.