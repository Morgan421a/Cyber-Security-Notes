- AKA **Bootstrap Loader**
- Also called **boot manager** if it provides an interactive menu with multiple boot choices

- Responsible for loading OS from the boot device into RAM to allow OS to startup and take control of components