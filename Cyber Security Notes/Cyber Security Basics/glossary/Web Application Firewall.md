---
aliases:
  - WAF
---
- A firewall that sits between the web requests from clients and the web server
	- Primarily used to detect hacking/attack attempts based on the configuration set by the admin 
	- Also analyses web requests to see if they come from a real browser instead of a bot
	- Uses rate limiting to check for an excessive number of requests from an IP address