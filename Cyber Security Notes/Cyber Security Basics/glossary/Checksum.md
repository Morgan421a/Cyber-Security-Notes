Field which provides integrity checking for protocols like TCP/IP.

Value will be different from expected if any data is changed and will corrupt

Done by initial device making maths calculation and remembering the output, when receiving device performs same calculation output must be the same as sent otherwise data must be corrupt